# 变更说明 S2-008：图片粘贴即时注入修复（2026-06-05）

概述：
- 修复了图片粘贴/拖拽后预览不显示的问题。通过在 `MainViewModel` 注册“待注入已保存图片”并在预览构建阶段将保存后的绝对路径原子注入到对应 `PreviewBlockViewModel` 中，避免异步赋值命中已被重建的 VM 导致图片丢失。

关键改动：
- `Muse/ViewModels/MainViewModel.cs`
  - 新增 `_pendingSavedImages` 队列与 `RegisterPendingSavedImage(relativePath, absolutePath)`。
  - 将 `BuildPreviewBlocks` 由 static 改为实例方法，在构造 VM 列表后尝试注入 pending 项。
- `Muse/Views/MainView.axaml.cs`
  - 在粘贴与拖拽保存图片成功后调用 `RegisterPendingSavedImage` 注册待注入项。
- `Muse/ViewModels/PreviewBlockViewModel.cs`
  - 保持使用 `AssignImagePath`/`SetImagePathAndBitmap` 在 UI 线程创建 `Bitmap`。

验证：
- 已执行 `dotnet build`，构建成功（有若干编译警告）。
- 下一步需运行应用并在编辑器中粘贴图片以收集运行时日志并确认 `ImageBitmap assigned` 的 VmId 与 `BuildPreviewBlocks` 日志中的 VmId 匹配。

后续建议：
- 为粘贴场景编写集成测试，模拟保存并刷新 preview，断言 Bitmap 已注入。
- 若仍出现间歇性问题，可把匹配策略从“文本包含 rel”升级为基于行号或文本锚点的更精确匹配。

作者：自动化助理（协助实现并记录）
日期：2026-06-05
