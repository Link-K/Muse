# 变更说明：S2-004 渲染块样式与预览交互

- 任务 ID：S2-004
- 任务名称：渲染块样式与预览交互（块卡片、代码块高亮、表格对齐、复制/锚点交互）
- 负责人：待分配
- 主模块：Muse（UI） / Muse.Editor.Rendering（渲染 DTO）
- 当前状态：已完成

## 已完成工作（摘要）

- 在 `PreviewBlockViewModel` 中实现 `CopyCodeCommand` 与 `CopyAnchorCommand`，并将复制逻辑通过 `IClipboardService` 抽象，保证无头测试环境可替代与 mock。
- 添加 `AvaloniaClipboardService` 作为运行时实现，并在测试中使用 `RecordingClipboardService` 验证复制行为。
- 为复制命令新增单元测试：`Muse.Tests/PreviewBlockViewModelTests.cs`，覆盖代码复制、标题锚点复制及非标题不触发剪贴板的场景。
- 解决 `GridSplitter` 在 headless 环境触发静态初始化的问题：引入 `SafeGridSplitter`（`Muse/Controls/SafeGridSplitter.cs`），在运行时（附加到视觉树时）创建真实 `GridSplitter`，避免测试期间查找 `ICursorFactory` 的异常。
- 将 XAML 中的 `GridSplitter` 替换为 `SafeGridSplitter`（`Muse/Views/MainView.axaml`），并验证 `dotnet test` 在 CI/本地 headless 环境下通过。
- 运行完整测试套件并验证：所有单元测试通过（目前 `dotnet test` 报告 58/58 通过）。
- 已在本地以 `Muse.Desktop` 实例进行了目视冒烟验证（已启动桌面并手动验证复制与锚点交互）。

## 变更目的
完善预览区的渲染块交互体验，使预览不仅展示渲染结果，还支持常用操作：
- 从代码块一键复制代码（Copy）
- 复制 heading 的锚点链接（Copy Anchor）
- 表格对齐与可替代纯文本展示
- 代码块语言显示与语法高亮准备（后续集成高亮库）

## 实施内容（分步）
1. 实现 `PreviewBlockViewModel.CopyCodeCommand`：调用 Avalonia 剪贴板 API，将 `Content` 写入剪贴板；在测试环境提供可替换的 `IClipboardService` 以便单元测试模拟。
2. 实现 `PreviewBlockViewModel.CopyAnchorCommand`：基于 Heading 文本生成锚点（slugify），并拷贝到剪贴板；同步显示短暂反馈（`SaveFeedbackMessage` 或局部提示）。
3. 表格样式完善：在 `PreviewTableRowTemplate` 中优化列间距与对齐，加入等宽字体或按需回退纯文本显示。已有模板已加入 `PreviewTableRowTemplate`，需在样式中保证边框与间隔一致。
4. 代码块交互：提供语言显示（已在 `PreviewBlockViewModel.CodeFenceLanguage` 中），后续接入语法高亮库（可选，S2-004 后半段）。
5. 单元测试：为复制命令添加单元测试（通过注入 `IClipboardService` 测试复制行为），并做手动冒烟测试验证 UI 交互（在本地桌面运行）。

## 验收结果

- 单元测试：`PreviewBlockViewModel` 复制命令相关测试已加入并通过（见 `Muse.Tests/PreviewBlockViewModelTests.cs`）。
- 集成验证：在本地 `Muse.Desktop` 中，代码复制与标题锚点复制功能可用；`GridSplitter` 的交互在桌面模式下正常工作，且不会导致 headless 测试异常。

## 后续动作

- 已开始下一个任务 `S2-006：标签拖拽与重排（可排序标签）`，将实现拖拽排序与持久化标签顺序。
- 若需回滚 `SafeGridSplitter` 的临时措施，可在 CI/Runner 上启用平台服务或替换为更完整的测试平台初始化方案（见 `files/实施计划.md` 风险条目）。

## 测试场景
- 代码块复制：将不同长度与包含换行的代码复制到剪贴板，校验剪贴板内容一致。
- 标题锚点复制：不同字符集标题（含空格/特殊符号）生成的锚点符合 slug 规则。
- 表格展示：对齐与分隔线在宽窄窗口下显示稳定。
- 无头 CI 兼容：确保单元测试不依赖真实系统剪贴板（通过 `IClipboardService` mock）。

## 验收标准
- `CopyCodeCommand` 与 `CopyAnchorCommand` 的单元测试通过。
- 在本地 `Muse.Desktop` 中功能可用并做手动验证（代码复制、锚点复制、表格显示）。
- 相关文档已更新（本文件与 `files/实施计划.md`）。

## 风险与缓解
- 风险：在 headless 测试环境中，UI 组件静态初始化可能需要平台服务（例如 `ICursorFactory`），导致测试失败。
  - 缓解：将复制逻辑抽象到 `IClipboardService` 并提供测试替代实现；对 `GridSplitter` 相关的 headless 初始化问题另开任务（S2-005 已处理）。

## 下一步（我将开始的工作）
- 在 `PreviewBlockViewModel` 中把占位 `CopyCodeCommand` 替换为真实实现，注入 `IClipboardService` 并添加针对性的单元测试。
- 提交小变更并运行 `dotnet test` 与本地 `Muse.Desktop` 冒烟验证。
