# 变更说明 S2-003

- PR: #3（feat/s2-003-skeletons）
- 合并到分支: main
- 功能概要:
  - 添加编辑器 Markdown 渲染适配器骨架 (`IMarkdownEngineAdapter`、`MarkdownEngineAdapter`)。
  - 实现 `TyporaBlockActivationService` 的输入校验逻辑与测试。
  - 添加 `AssetService.SaveImageAsync` 的实现与测试。
  - 添加 `ExportService`（使用 Markdig 渲染）与测试，并更新 `Muse.Export.csproj` 的依赖。
  - 添加简单的内存 RAG 索引实现 `InMemoryIndexBuilder` 与对应测试。
- 测试:
  - 本地运行 `dotnet test` 报告：74 个测试通过，0 个失败（合并前已验证）。
- 相关文件（部分）:
  - Muse.Editor.Rendering/IMarkdownEngineAdapter.cs
  - Muse.Editor.Rendering/MarkdownEngineAdapter.cs
  - Muse.Editor.Core/TyporaBlockActivationService.cs
  - Muse.Assets/AssetService.cs
  - Muse.Export/ExportService.cs
  - Muse.AI.RAG/VectorIndex/InMemoryIndexBuilder.cs
  - 多个测试项目中的新增/更新测试文件
- 回滚说明:
  - 若需回滚，请使用 `git revert -m 1 <merge-commit>` 或从 PR 页面使用 GitHub 的 Revert 功能。

---
自动生成于: 2026-06-02
